# Production Environment Configuration

# Server Settings
HOST=0.0.0.0
PORT=8080
WORKERS=4

# Redis Configuration (Optional - for distributed job management)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Directory Settings
OUTPUT_DIR=/var/lib/clipping-api/output
MEDIA_LIBRARY_DIR=/mnt/media

# Security Settings
ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Additional Media Roots (colon-separated)
ADDITIONAL_MEDIA_ROOTS=/mnt/qnap/media_eng/indexed_media:/mnt/qnap/media_kor

# Performance Settings
MAX_WORKERS=4  # Thread pool size for video processing

# Logging
LOG_LEVEL=INFO